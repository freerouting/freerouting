title=Manual Rules
via_rule=via rule:
via_rule_tooltip=Via rule used for layer changes during manual routing.
trace_clearance_class=trace clearance class:
trace_clearance_class_tooltip=Clearance class used by traces during manual routing.
trace_width=trace width:
trace_width_tooltip=Trace width used for manual routing.
on_layer=on layer:
on_layer_tooltip=Layer on which to apply the manual trace width.
